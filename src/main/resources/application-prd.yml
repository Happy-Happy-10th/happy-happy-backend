spring:
  datasource:
    url: jdbc:mysql://49.50.133.140:3306/yottaeyo-db
    username: yottaeyo
    password: yottaeyo1234@@
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: yotaeyo1@gmail.com
    password: mgirygjzjvoqklus
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    date-format: yyyy-MM-dd'T'HH:mm:ss
    time-zone: Asia/Seoul
  session:
    store-type: redis
    redis:
      flush-mode: on_save
      namespace: spring:session
      save-mode: on_set_attribute
  data:
    redis:
      host: 172.18.0.3
      port: 6379
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              - account_email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v2/userinfo
            user-name-attribute: id
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
api:
  holiday:
    serviceKey: "+ofyrfSLLZl4U3tsLiF2wKxnnIgQ1ZEZu4TIKdkTmKmct4orBWKVCpVaBvTqoIfGhSAlvVCSDV34tsEYEz9BbA=="

server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: false
swyp:
  jwt:
    secret: 04b0d5112f1afbdc562c552e9ee358619ffd8ed5a9ba41104c6654a2
    access-token-validity-in-milli-seconds: 86400000 # 1일
    refresh-token-validity-in-milli-seconds: 86400000 # 1일
clova:
  auth:
    token: nv-ea34df30969a4139afd2c19c92d74ff4q8YE

logging:
  level:
    org.springframework.security.oauth2: DEBUG
    org.springframework.security: DEBUG
    com.happyhappy.backend: DEBUG
    root: INFO
    org.springframework.boot.context.logging: INFO